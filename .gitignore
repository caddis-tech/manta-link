__pycache__/
*.pyc

# Tooling caches and editable-install metadata
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# The API token, once there is one. Never committed: it is a per-boat device
# credential, and the only copy that should exist is the caddis-api Device row.
.env
